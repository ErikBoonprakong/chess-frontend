import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9c8f41f2",
    },
    secondary: {
      main: "#9c8f41f2",
    },
  },
});

export default theme;
